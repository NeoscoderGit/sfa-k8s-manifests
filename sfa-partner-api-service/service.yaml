apiVersion: v1
kind: Service
metadata:
  name: sfa-partner-api-service
  namespace: sfa-partner-api-service
spec:
  selector:
    app: sfa-partner-api-service
  ports:
  - port: 80
    targetPort: 8000
  type: ClusterIP
