apiVersion: apps/v1
kind: Deployment
metadata:
  name: sfa-master-data-service
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sfa-master-data-service
  template:
    metadata:
      labels:
        app: sfa-master-data-service
    spec:
      imagePullSecrets:
      - name: harbor-secret
      containers:
      - name: sfa-master-data-service
        image: 10.10.10.202/pico/sfa-master-data-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: sfa-master-data-service
  namespace: default
spec:
  selector:
    app: sfa-master-data-service
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
