apiVersion: v1
kind: Service
metadata:
  name: sfa-master-data-service
  namespace: sfa-master-data-service
spec:
  selector:
    app: sfa-master-data-service
  ports:
  - port: 80
    targetPort: 8003
  type: ClusterIP
