apiVersion: apps/v1
kind: Deployment
metadata:
  name: sfa-app-service
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sfa-app-service
  template:
    metadata:
      labels:
        app: sfa-app-service
    spec:
      imagePullSecrets:
      - name: harbor-secret
      containers:
      - name: sfa-app-service
        image: 10.10.10.202/pico/sfa-app-service:v1
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: sfa-app-service
  namespace: default
spec:
  selector:
    app: sfa-app-service
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
