apiVersion: apps/v1
kind: Deployment
metadata:
  name: sfa-app-service
  namespace: sfa-app-service
spec:
  replicas: 5
  selector:
    matchLabels:
      app: sfa-app-service
  template:
    metadata:
      labels:
        app: sfa-app-service
    spec:
      imagePullSecrets:
      - name: harbor-registry-secret
      containers:
      - name: sfa-app-service
        image: 10.10.10.202/pico/sfa-app-service:v2
        imagePullPolicy: Always
        ports:
        - containerPort: 8001
