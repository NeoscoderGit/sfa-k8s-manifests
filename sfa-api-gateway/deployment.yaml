apiVersion: apps/v1
kind: Deployment
metadata:
  name: sfa-api-gateway
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sfa-api-gateway
  template:
    metadata:
      labels:
        app: sfa-api-gateway
    spec:
      imagePullSecrets:
      - name: harbor-secret
      containers:
      - name: sfa-api-gateway
        image: 10.10.10.202/pico/sfa-api-gateway:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: sfa-api-gateway
  namespace: default
spec:
  selector:
    app: sfa-api-gateway
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
