apiVersion: v1
kind: Service
metadata:
  name: sfa-sales-management-service
  namespace: sfa-sales-management-service
spec:
  selector:
    app: sfa-sales-management-service
  ports:
  - port: 80
    targetPort: 8003
  type: ClusterIP
