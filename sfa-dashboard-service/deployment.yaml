apiVersion: apps/v1
kind: Deployment
metadata:
  name: sfa-dashboard-service
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sfa-dashboard-service
  template:
    metadata:
      labels:
        app: sfa-dashboard-service
    spec:
      imagePullSecrets:
      - name: harbor-secret
      containers:
      - name: sfa-dashboard-service
        image: 10.10.10.202/pico/sfa-dashboard-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: sfa-dashboard-service
  namespace: default
spec:
  selector:
    app: sfa-dashboard-service
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
