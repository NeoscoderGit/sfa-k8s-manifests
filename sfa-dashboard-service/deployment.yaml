apiVersion: apps/v1
kind: Deployment
metadata:
  name: sfa-dashboard-service
  namespace: sfa-dashboard-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sfa-dashboard-service
  template:
    metadata:
      labels:
        app: sfa-dashboard-service
    spec:
      containers:
      - name: sfa-dashboard-service
        image: 10.10.10.202/pico/sfa-dashboard-service:v1
        ports:
        - containerPort: 8001
      imagePullSecrets:
      - name: harbor-registry-secret
