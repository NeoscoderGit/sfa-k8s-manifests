apiVersion: v1
kind: Service
metadata:
  name: sfa-dashboard-service
  namespace: sfa-dashboard-service
spec:
  selector:
    app: sfa-dashboard-service
  ports:
  - port: 80
    targetPort: 8001
  type: ClusterIP
