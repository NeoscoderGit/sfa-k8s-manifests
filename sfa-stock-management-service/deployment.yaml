apiVersion: apps/v1
kind: Deployment
metadata:
  name: sfa-stock-management-service
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sfa-stock-management-service
  template:
    metadata:
      labels:
        app: sfa-stock-management-service
    spec:
      imagePullSecrets:
      - name: harbor-secret
      containers:
      - name: sfa-stock-management-service
        image: 10.10.10.202/pico/sfa-stock-management-service:v1
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: sfa-stock-management-service
  namespace: default
spec:
  selector:
    app: sfa-stock-management-service
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
