apiVersion: apps/v1
kind: Deployment
metadata:
  name: sfa-survey-service
  namespace: sfa-survey-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sfa-survey-service
  template:
    metadata:
      labels:
        app: sfa-survey-service
    spec:
      imagePullSecrets:
      - name: harbor-registry-secret
      containers:
      - name: sfa-survey-service
        image: 10.10.10.202/pico/sfa-survey-service:v2
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: sfa-survey-service
  namespace: sfa-survey-service
spec:
  selector:
    app: sfa-survey-service
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
