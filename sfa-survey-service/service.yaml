apiVersion: v1
kind: Service
metadata:
  name: sfa-survey-service
  namespace: sfa-survey-service
spec:
  selector:
    app: sfa-survey-service
  ports:
  - port: 80
    targetPort: 8003
  type: ClusterIP
