apiVersion: apps/v1
kind: Deployment
metadata:
  name: sfa-shipment-api-service
  namespace: sfa-shipment-api-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sfa-shipment-api-service
  template:
    metadata:
      labels:
        app: sfa-shipment-api-service
    spec:
      containers:
      - name: sfa-shipment-api-service
        image: 10.10.10.202/pico/sfa-shipment-api-service:v1
        ports:
        - containerPort: 5040
      imagePullSecrets:
      - name: harbor-registry-secret
